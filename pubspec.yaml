name: comvalglo
description: A Flutter website to promote a school project
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.0.5
  firebase: ^7.3.0 # For web
  firebase_core: ^0.4.4+3 # For native
  firebase_storage: ^3.1.5
  font_awesome_flutter: ^8.8.1
  google_fonts: ^0.4.0
  carousel_slider: ^1.4.1
  circle_wheel_scroll: ^0.0.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - images/
